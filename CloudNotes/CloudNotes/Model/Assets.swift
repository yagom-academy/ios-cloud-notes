import UIKit

enum Assets {
    static var plusImage: UIImage? {
        return UIImage(systemName: "plus")
    }
    static var ellipsisCircleImage: UIImage? {
        return UIImage(systemName: "ellipsis.circle")
    }
    static var sampleData: NSDataAsset? {
        return NSDataAsset(name: "sample")
    }
}
